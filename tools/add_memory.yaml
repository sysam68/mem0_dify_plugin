identity:
  name: add_mem0ai_memory
  author: beersoccer
  label:
    en_US: Add Memory
    zh_Hans: 记忆添加
description:
  human:
    en_US: Add memory node
    zh_Hans: 记忆添加节点
  llm: Add memory node
parameters:
  - name: user
    type: string
    required: true
    label:
      en_US: User Message
      zh_Hans: 用户消息
    human_description:
      en_US: The message from the user in the conversation.
      zh_Hans: 对话中的用户消息。
    llm_description: The message from the user in the conversation.
    form: llm
  - name: assistant
    type: string
    required: true
    label:
      en_US: Assistant Response
      zh_Hans: 助手回应
    human_description:
      en_US: The response from the assistant in the conversation.
      zh_Hans: 对话中的助手回应。
    llm_description: The response from the assistant in the conversation.
    form: llm
  - name: user_id
    type: string
    required: true
    label:
      en_US: User ID
      zh_Hans: 用户ID
    human_description:
      en_US: Required. The unique user identifier.
      zh_Hans: 必填。用户的唯一标识符。
    llm_description: Required user identifier.
    form: llm
  - name: agent_id
    type: string
    required: false
    label:
      en_US: Agent ID
      zh_Hans: 代理ID
    human_description:
      en_US: Optional agent identifier.
      zh_Hans: 可选的代理标识符。
    llm_description: Optional agent identifier.
    form: llm
  - name: app_id
    type: string
    required: false
    label:
      en_US: App ID
      zh_Hans: 应用ID
    human_description:
      en_US: Optional application identifier (SaaS only; ignored in Local mode).
      zh_Hans: 可选的应用标识符（仅 SaaS 模式有效，本地模式忽略）。
    llm_description: Optional application identifier (SaaS only).
    form: llm
  - name: run_id
    type: string
    required: false
    label:
      en_US: Run ID
      zh_Hans: 运行ID
    human_description:
      en_US: Optional run identifier.
      zh_Hans: 可选的运行标识符。
    llm_description: Optional run identifier.
    form: llm
  - name: metadata
    type: string
    required: false
    label:
      en_US: Metadata (JSON)
      zh_Hans: 元数据（JSON）
    human_description:
      en_US: 'Optional metadata as JSON string (e.g., {"category": "food", "priority": "high"})'
      zh_Hans: '可选的元数据，JSON 字符串格式（例如：{"category": "food", "priority": "high"})'
    llm_description: Optional metadata as JSON string
    form: llm
  - name: output_format
    type: select
    required: false
    label:
      en_US: Output Format Version
      zh_Hans: 输出格式版本
    human_description:
      en_US: The version of the output format (v1.0, v1.1, or v2). Default is v1.1.
      zh_Hans: 输出格式的版本（v1.0、v1.1 或 v2）。默认为 v1.1。
    llm_description: The version of the output format. Default is v1.1.
    form: form
    options:
      - value: v1.0
        label:
          en_US: v1.0
          zh_Hans: v1.0
      - value: v1.1
        label:
          en_US: v1.1 (default)
          zh_Hans: v1.1（默认）
      - value: v2
        label:
          en_US: v2
          zh_Hans: v2
extra:
  python:
    source: tools/add_memory.py
