# -------------------------------------------------------------------
# 1. 版本控制和构建系统目录 (必须排除)
# -------------------------------------------------------------------
.git/          # Git 版本控制历史 (体积巨大，不需要分析)
.svn/          # Subversion 版本控制
.hg/           # Mercurial 版本控制
.idea/         # JetBrains IDE (IntelliJ, PyCharm) 配置
.vscode/       # VS Code / Cursor 配置 (除非需要 AI 分析配置)
.cache/
.DS_Store      # macOS 系统文件
Thumbs.db      # Windows 系统文件

# -------------------------------------------------------------------
# 2. Python 虚拟环境和缓存 (必须排除)
# -------------------------------------------------------------------
# Python 虚拟环境目录
venv/
.venv/
env/
.env/

# Python 编译和运行时缓存
__pycache__/
*.pyc
*.pyd
*.o
*.so

# Python 包管理工具的缓存
.eggs/
*.egg-info/

# -------------------------------------------------------------------
# 3. Dify 插件和相关工具的特定目录 (针对 Dify 项目)
# -------------------------------------------------------------------
# 插件运行时的临时环境或缓存
.dify_plugin_envs/
.mem0_storage/       # 如果 mem0 库将本地数据存储在这里
_assets/

# 日志文件
logs/
*.log

# -------------------------------------------------------------------
# 4. 依赖和输出目录 (针对所有项目)
# -------------------------------------------------------------------
# JavaScript / Web (如果你的插件包含前端或 Node.js 依赖)
node_modules/

# 编译/构建输出
dist/
build/

# 自动生成的文档或测试覆盖率报告
docs/
htmlcov/
.coverage

# -------------------------------------------------------------------
# 5. 敏感信息文件 (可选，但推荐)
# -------------------------------------------------------------------
# 实际的 .env 文件 (包含敏感 API Key，不应被 LLM 读取)
.env